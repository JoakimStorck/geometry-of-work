from __future__ import annotations

# Extensions we currently consider as "tables" in db_*/ directories.
DEFAULT_TABLE_EXTS = (".txt",)

# Table basenames to ignore when scanning a db directory (no extension).
IGNORED_TABLES = {
    "Read Me",
}
